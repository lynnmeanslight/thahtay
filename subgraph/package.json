{
  "name": "thahtay-hook-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy thahtay-hook/1.0.0 --from-abi .",
    "deploy:studio": "graph deploy --studio thahtay-hook"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
